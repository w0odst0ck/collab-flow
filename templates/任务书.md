# 任务书模板（executor 输入）

> 用途：critic 审查通过后，把设计翻译成 executor 可执行的任务书（翻译层二次设计）
> 使用：复制模板填内容；**翻译不出来 = 方案粒度不够 = 打回 designer**

---

【任务：<一句话，含「按方案实现，不改设计」声明>】

【背景】
<为什么做这个任务；上下文；相关文件路径>

【目标】
<交付物清单，具体到文件路径>

【约束】
- <技术栈/风格约束>
- <关键实现要求>
- 参考 <已有封装模式>

【红线】
- <不改什么/不碰什么>
- <测试边界（stub/临时目录/不消耗真实资源）>
- **执行器契约红线**：执行者不得在项目根/工作区生成 `result.json` 等契约文件（`result.json`/`diff.patch`/`cost.json` 由 wrapper 统一生成到 `executor/` 目录）；任务需要产出结果文件时，严格写到任务书指定的路径，未指定则不产生；中间产物放临时目录（`/tmp` 或 `.flow/` 下），用完即清，不残留项目根

【审查补充意见（实现时必须落实）】
1. <critic 审查时提出的补充点>
2. ...

【自查提示（执行者必须遵守）】
- 遇到 bash 语法错误：用 `bash -n` + **逐行删除法**定位（删掉某行后语法变绿即元凶），**禁止写词法分析器/状态机**调试
- 实现完成后自查：① 所有脚本有可执行权限（chmod +x）② 数值字段的 0 值不丢失（用 `is not None` 判断，勿用 `or ""` 吞 0）③ 管道取退出码用 PIPESTATUS 或重定向
- ⚠️ **本文件下方 ```flow 前置块是受限 YAML**：`test_command`/`diff_scope` 禁用 `*`（glob）、`&&`、`|`、`>`、`{`、`}`、`[`、`]`、`$`——`test_*.py` 这类 glob 会整个块解析失败（verify 报 scope_undeclared）；测试命令用 `python3 -m unittest discover -s tests` 最稳
- ⚠️ **diff_scope.allow 必须穷举所有将改文件**（含“顺带”改的：`__init__.py` 注册、`.gitignore`）——漏一个 verify 就 out_of_scope 打回；执行者补的合理文件可加回 allow 重验

【```flow 前置块（可选，机器可读结构化声明）】
> executor 与 `verify --auto` 读取；缺失不报错但触发回退（fail-closed）。可声明 `test_command` 与 `diff_scope`（allow/deny 为标量序列）：

````
```flow
test_command: python3 -m unittest discover tests
diff_scope:
  allow:
    - scripts/flow-core.py
  deny:
    - scripts/dsh-design
```
````

【验收标准】
1. <测试命令> 全过（<用例数> 用例）
2. 所有脚本可执行权限 ✓ + 0 值字段不丢 ✓
3. <独立验证：手动冒烟场景>
4. <对照方案错误处理表：每个错误场景有对应处理 + 测试覆盖>
5. <输出文件清单与方案交付物一致>
