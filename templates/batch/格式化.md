# 批处理任务：格式化

> 执行器：script（零 LLM 秒级）｜ command 行必填
> 用法：替换 command 为实际格式化命令（注意：黑名单拒绝 rm -rf 等危险命令）

command: <格式化命令，如 ruff check --fix 或 black 目标目录>

## 说明

- 在 workdir 内执行（cd workdir）
- 超时默认 300s（timeout: N 可覆盖）
