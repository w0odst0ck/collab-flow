【任务：实现 collab-flow P2（work-item + 状态机 + engine/store）（方案已设计定稿，按方案实现，不改设计）】

【背景】
collab-flow 开源框架 P0/P1 已完成。P2 实现框架核心：work-item 约定 + 显式状态机 + 工件存储 + flow CLI。完整方案已通过审查：designs/20260814-134840-956887393-P2-workitem.md（579 行，含全部 schema/转移表/伪代码/测试用例）。你按方案实现，不改设计。

【目标】
在项目根目录实现（交付物）：
1. scripts/flow —— bash 薄壳 CLI（命令分发 + --json 包装 + exit 透传）
2. scripts/flow-core.py —— python 核心：状态机纯函数 TRANSITIONS/transition()/evaluate_guard + store 层（save_status_atomic/append_event/with_workitem_lock/acquire_lock）+ 受限 YAML 解析器（支持映射+list-of-mapping+标量）+ 命令实现
3. config/defaults.yaml —— 新增 workitem:/gates: 标量块（见方案 §7.1，P1 解析器兼容）
4. tests/test-flow-core.py —— 状态机单测 T1-T24 + store 测试 S1-S9（python3 -m unittest）
5. tests/run-flow-smoke.sh —— CLI 冒烟 C1-C18（stub 零 API）
6. .github/workflows/ci.yml —— 追加 P2 检查（unittest + run-flow-smoke + flow 相关 bash -n/py_compile/deny-list）

【约束】
- bash + python3 stdlib-only（零新依赖，禁 jq/PyYAML；YAML 用自包含受限解析器）
- 状态机转移表/守卫/原因码严格按方案 §2（含 effects 语义：iteration/same_defect_count/takeover/re_execute_count）
- store 严格按方案 §4：原子写（temp+fsync+os.replace）、events.jsonl flock 追加（seq=追加前行数+1，**读取与追加在同一 flock 内**）、双层锁（本地 flock + 逻辑锁 TTL 过期自动释放）
- CLI 严格按方案 §3：命令族/参数/消歧表/退出码 0/1/2/124/--json 输出契约
- 门禁守卫按方案 §5：守卫注册表 + build_ctx + decision/verify/guard 子命令（模拟验证，P2 不接真实执行器）
- id 正则硬编码：^[a-z0-9]+(-[a-z0-9]+)*$（1..64），拒绝路径穿越
- actor/owner 默认 flow:<plane_id>（config workitem.plane_id，默认 control），绝不取 hostname/用户名
- 锁 TTL 默认 3600（workitem.lock_ttl_s），可 env FLOW_LOCK_TTL_S 覆盖
- --force 语义：跳过守卫但不跳过转移形状合法性与锁（方案 §3.3）

【红线】
- **P1 文件零改动**：scripts/dsh-design、flow-config、flow-config-core.py、dsh-design-stats.py 一字不动；tests/run-smoke.sh、run-config-smoke.sh 断言不动（只允许 CI 文件追加）
- config/defaults.yaml 只新增 workitem:/gates: 标量块（不修改已有字段）
- 测试全程临时目录/零 API；不碰真实 .flow/
- 新增内容零个人标识（人名/真实路径/真实项目名），deny-list：/home/[^~]|sk-[A-Za-z0-9]{10} 零命中

【审查补充意见（必须落实）】
1. bash 薄壳 flow 保持极薄：只做命令分发与 --json 包装，参数解析全部下沉 core.py（薄壳内部直接调 python3 flow-core.py <sub> ...）
2. events.jsonl 的 seq 计算必须在 flock 锁内完成（读取行数+追加 同一临界区），S3 并发用例验证
3. verify.json route 字段由 CLI --route 写入（P2 模拟），schema 严格按方案 §1.6

【自查提示（v2 模板）】
- bash 语法错误：bash -n + 逐行删除法定位，禁止写词法分析器
- 完成后自查：① 新脚本 chmod +x ② 0 值字段不丢（is not None）③ 管道退出码用 PIPESTATUS
- 纯函数与 I/O 分离：transition()/evaluate_guard() 零文件 I/O（单测直接 import 调用）

【验收标准】
1. python3 -m unittest discover tests 全绿（T1-T24 + S1-S9）
2. bash tests/run-flow-smoke.sh 全绿（C1-C18）
3. bash tests/run-smoke.sh（15）+ run-config-smoke.sh（17）仍全绿（回归红线）
4. bash -n scripts/flow tests/run-flow-smoke.sh + python3 -m py_compile scripts/flow-core.py 通过
5. 完整状态机路径手工冒烟：new → decision(reject) → reviewed → translated 拒绝；decision(pass) 后放行 → … → retrospected；log 事件与转移一一对应
6. deny-list 零命中（scripts/flow scripts/flow-core.py config/defaults.yaml）
7. 报告：交付物清单 + 每项验收结果 + git diff 统计（P1 文件零改动证明）
