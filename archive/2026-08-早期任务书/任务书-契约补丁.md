# 任务书：执行器契约补丁 — result.json 落盘位置约束

> 项目：/home/l/.openclaw/workspace/projects/collab-flow
> 日期：2026-08-15 ｜ 执行：reasonix（你是执行者，直接改，不改无关文件）
> 背景：2026-08-15 mojin-intel 首跑暴露第 6 坑——reasonix 执行任务时把 result.json 写到**项目根**（intel/result.json），而非契约位置 executor/result.json，导致手工挪/删。wrapper.sh 自身已正确生成 $OUT/result.json（契约位），问题在**任务书模板没约束执行者**，reasonix 自由发挥。

## 需求（按方案实现，不改设计）

1. 在 `templates/任务书.md` 的【红线】或【自查提示】中补充**执行器契约红线**（明确文本，grep 可查）：
   - 执行者**不得**在项目根/工作区生成 `result.json` 等契约文件（契约产物由 wrapper 统一生成到 executor/ 目录）
   - 任务需要产出结果文件时，严格写到任务书指定的路径；未指定则不产生
   - 中间产物放临时目录（/tmp 或 .flow/ 下），用完即清，不残留项目根
2. 检查 `executors/reasonix/spec.yaml`：若已有执行器契约/产物说明则同步补一句（指向模板红线）；若 spec 不含此类说明则不改 spec（只改模板）
3. **不改动 wrapper.sh 逻辑**（它是对的）；纯模板/文档层契约补强

## 约束

- 只改 templates/任务书.md（+ 可选 spec.yaml 一行补充）
- 不动 flow-core.py / scripts/ / tests/ 现有逻辑
- 不破坏模板的 flow 前置块解析（```flow 块格式不变）

## 验收标准

1. `grep -n "result.json" templates/任务书.md` 能查到契约红线（≥1 处）
2. 现有测试全过：`bash tests/run-smoke.sh`（或按 README 指示的测试入口）全绿
3. 模板其余部分无改动痕迹（git diff 仅新增红线段落）
4. result.json 写到 `executor/result.json` 契约位置（wrapper 行为，不改）——任务书明确"执行者不生成该文件到项目根"
