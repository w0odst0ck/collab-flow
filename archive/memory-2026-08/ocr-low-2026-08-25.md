# ocr low findings 留档（2026-08-25）

> 按分级收口规则（2026-08-25 固化）：low 不阻塞 commit，落盘留档，后续按需处理。
> 来源：collab-flow 多轮 ocr review 中未修复的 low 级 findings。

## 本轮（ocr10，2026-08-25）

### L1 tests/test-flow-task-serial.py:83（maintainability）
上下文管理器用 os.dup2 改进程级 stdout/stderr fd，`__exit__` 的恢复序列无 try/finally 保护——中间 os.dup2 抛异常时 fd 状态不一致。

### L2 tests/test-flow-task-serial.py:76（maintainability）
`__enter__` 在 os.dup 与 os.dup2 之间抛异常（如 os.open 失败）时，重定向 fd 和 guard 状态残留不一致。

> 处理建议：两个 finding 同源（test-flow-task-serial.py 的 fd 重定向 context manager），后续一起修——加 try/finally 或改用 contextlib.redirect_stdout 替代。

## 历史遗留（此前轮次已修/已随轮次处理，仅登记）

- ocr9（21:31 作废轮）3 条：Windows select 兼容 / 死参数 t / env flag 绕过——其中 env flag 绕过与后续轮次 high 相关，已由 round8/9 修复覆盖，剩余不适用
- 早期轮次 low：同名 helper 分叉、chunk 元数据、budget 截断、测试编号、fixture 格式等——已在对应轮次随修或验证不适用
