【任务：实现 collab-flow P3（执行器适配 + verify 自动化）（方案已设计定稿，按方案实现，不改设计）】

【背景】
collab-flow 框架 P0/P1/P2 已完成。P3 是端到端自动化的最后一块：接真实设计器（dsh-design）与真实执行器（reasonix）进状态机，新增 design/execute 子命令，verify 升级为真实测试门（--auto）。完整方案已通过审查：designs/20260814-144846-719136451-P3-executor.md（586 行）。你按方案实现，不改设计。

【目标】
在项目根目录实现（交付物）：
1. scripts/flow-core.py —— 扩展：+design/execute 子命令 + verify --auto + executor 适配层（load_executor_spec/run_executor/run_designer）+ 受限 YAML 解析器 list-of-scalar 向后兼容扩展 + 抽取共享辅助 _do_transition（不改变现有 transition 行为）
2. scripts/flow —— 仅 usage/help 文案追加 design/execute 两行（逻辑零改动）
3. executors/reasonix/{spec.yaml, wrapper.sh} —— 默认执行器适配器（reasonix 经 rx，~50 行 wrapper：cd/timeout/rx/git diff/redact/写 result.json+diff.patch）
4. executors/stub/{spec.yaml, wrapper.sh} —— 契约测试 stub（零 API，env 控制：STUB_EXECUTOR_EXIT/CHANGE/SLEEP/LEAK_SK/NO_RESULT/NO_DIFF）
5. config/defaults.yaml —— 新增 executor: 标量块（default/timeout_s/diff_scope 默认策略）
6. templates/任务书.md —— 新增 ```flow 前置块说明（test_command/diff_scope，可选）
7. tests/test-executor.py —— E1-E10 + D1-D3 + V1-V9 = 22 契约/门禁用例（python3 -m unittest，零 API 临时目录）
8. tests/run-executor-smoke.sh —— C19-C26 CLI 冒烟（stub 全链路）
9. .github/workflows/ci.yml —— 追加 P3 检查（bash -n/shellcheck/py_compile/unittest/run-executor-smoke/deny-list）

【约束】
- bash + python3 stdlib-only（零新依赖，禁 jq/PyYAML）
- 严格按方案 §2 三命令语义：design（调 flow-config -d workdir -o tmp --json brief.md → 落 design.md + design-result.json → created→designed）/ execute（spec 校验 runtime.os + sandbox==workspace-write → wrapper 调用 → result.json 分流 ok/failed/timeout → 转移或不转移）/ verify --auto（测试命令分层解析 → diff scope 核对 → 错误表核对 → verify.json+verify.md → 路由/不转移）
- **裸 verify（无 --auto）必须保持 P2 语义**（--tests/--diff/--errors 默认全 true，产出 all-pass）——run-flow-smoke.sh C14 依赖
- route 推导严格按方案 §4.4 表；--route 可覆盖
- 测试命令识别分层：--test-command > result.json.test_command > taskbook ```flow 前置块 > 惯例探测（Makefile/package.json 只做保守文件匹配，不执行）> 无 → fail-closed（command_unresolved）
- diff scope：--scope FILE > taskbook 前置块 diff_scope > 无声明 → fail-closed（scope_undeclared）；deny 命中立即 out_of_scope（含 P1 冻结文件 scripts/dsh-design 等）
- 错误表核对：markdown 表格解析（E\d+ 行），结构覆盖自动（处理+测试覆盖非空）、语义正确性归 critic（verify.md 列 human_review）；解析失败 → error_table_missing fail-closed
- 执行器沙箱 workspace-write（spec 校验：read-only 非法 exit 2）；wrapper 不读不传 key，日志过 sk- deny-list 防御性 redact
- 退出码 0/1/2/124；--json 契约沿用（成功 stdout 单行、失败 stderr 单行）
- design/execute/verify 复用锁与落盘路径（with_workitem_lock 内读→判→transition→append_event→save_status_atomic；执行器子进程运行期间不持锁）
- 零个人标识：deny-list /home/[^~]|sk-[A-Za-z0-9]{10} 对全部新增文件零命中

【红线】
- P1 文件零改动：scripts/dsh-design、flow-config、flow-config-core.py、dsh-design-stats.py 一字不动；tests/run-smoke.sh、run-config-smoke.sh 断言不动
- P2 既有行为不变：现有 11 子命令行为 + tests/test-flow-core.py(33) + tests/run-flow-smoke.sh(C1-C18) 全绿不改（flow-core.py 只做增量扩展）
- 测试全程 stub/临时目录/零 API；真实 reasonix 调用只做手动 E2E（不在 CI，不做进自动化测试）
- 不引入中心服务器/外部服务

【审查补充意见（必须落实）】
1. design --force 重跑时 design-result.json 同步覆盖（与 design.md 一致性）
2. test_command 惯例探测（Makefile/package.json/pyproject）只读文件保守匹配 target 名，绝不执行 make/npm
3. error_table 的 markdown 表格解析对格式变化健壮：分隔行/列数不一致容忍，整表不可解析 → error_table_missing（fail-closed）

【自查提示（v2 模板）】
- bash 语法错误：bash -n + 逐行删除法定位，禁止写词法分析器
- 完成后自查：① 新脚本 chmod +x（executors/*/wrapper.sh、run-executor-smoke.sh）② 0 值字段不丢（is not None）③ 管道退出码用 PIPESTATUS
- 纯函数与 I/O 分离：load_executor_spec/resolve_test_command/check_diff_scope/check_error_table 可独立 import 单测

【验收标准】
1. python3 -m unittest discover tests 全绿（33 既有 + 22 新增 = 55）
2. bash tests/run-executor-smoke.sh 全绿（C19-C26）
3. 回归：run-smoke.sh(15) + run-config-smoke.sh(17) + run-flow-smoke.sh(C1-C18) 全绿不改
4. bash -n 全部新脚本 + python3 -m py_compile flow-core.py 通过
5. 全链路 stub 冒烟：new → design(stub dsh) → review → translate → execute(stub) → verify --auto 通过 → verified；失败路由（测试 fail → impl 回退）正确
6. deny-list 零命中（新增文件全部）
7. git diff 证明 P1 文件零改动 + 11 子命令行为不变
8. 报告：交付物清单 + 每项验收结果 + 手动 E2E 步骤说明（§6.5 方案）
