# ocr low findings（2026-08-29，不阻塞 commit 留档）

来源：`ocr review` 三轮（session fb04e5a6 / 后续），cost-weekly-report + cost-budget-guard 两 workitem 合并审。

## 已修（本轮）
- cost-report.py `_tz()` no-op try/except（maintainability medium）→ 删
- flow-task-core.py pump 补选循环重复提升/超槽（bug high ×2）→ exclude 已提升+已暂停 + room 截断 + 单槽/多长任务测试 ×2
- cost-report.py exchange_rate/week_start_dow `inf` 未防护（bug medium ×2）→ `math.isfinite` 校验
- flow-task-core.py budget 未启用零扫描快速路径（performance medium ×3）→ 解 3 条
- tests/test_flow_task_budget.py 恒真断言（test low）→ 删

## 残留 low（不修，按分级收口规则留档）
1. **scripts/cost-report-weekly.sh:6** `set -uo pipefail` 缺 `-e`，且该脚本无管道 → pipefail 无效。
   - 评估：脚本是周报入口 wrapper，主逻辑在 cost-report.py；缺 `-e` 属防御性不足，不影响当前正确性（python 失败由 wrapper exit code 传播）。列入后续 shell 加固（如加 `-e` 需确认不掩盖 python 退出码语义）。
2. **scripts/cost-report.py:53** `_PROJECT_RE` 硬编码 `/workspace/projects/` 前缀，repo 迁移后项目识别可能偏。
   - 评估：本项目目录结构稳定，迁移时会同步改；标记为已知限制。
3. **scripts/cost-report.py:245-247** 注释称 fallback "非写死来源" 但实际写死 2.0。
   - 评估：与 pricing.yaml 全段 peak=2×offpeak 一致（注释已说明），文档措辞可后续微调。
4. **scripts/cost-report.py:227** 负 cost_usd 静默置 0.0。
   - 评估：fail-closed 设计（E11），负值不应出现，置 0 保守合理。
5. **scripts/cost-report.py:565-566** `week_timezone` 配置加载时不校验，运行时 ZoneInfoNotFoundError 才兜底。
   - 评估：E8 已有运行时回退 + warning，行为正确；如需 fail-fast 可后续加。
6. **tests/test-cost-report.py:493** 文本报告断言硬编码汇率 ¥3.60 = 0.50 × 7.2。
   - 评估：测试内固定 fixture 值，非实现问题。

## 结论
high/medium 清零 ✓；low 6 条留档，不阻塞 commit（符合 2026-08-25 分级收口规则）。
