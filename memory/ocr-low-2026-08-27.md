# ocr-low 留档 — 2026-08-27（ocr review 一轮：71e9980..HEAD，15 findings）

> 用户指示：只跑一轮修复，明早 review。high/medium 已修，low + 部分 medium 落盘。
> ocr session：见当日 review（15 findings / 13 items）

## 已修（high + medium 核心）

| finding | 级别 | 修复 |
|---|---|---|
| flow-core model=None → "--model None"（script 任务 execute 会失败）| high | 2972/3505 条件拼接（None 省略），1559/3231 已有 |
| script/run.sh 黑名单可绕过 | high | 多模式独立检查 + 绝对路径/cd 根/反斜杠变体；9 用例验证 |
| script result.json 契约缺 test_command | high | 任务书 test_command: 行 → result.json（verify 可解析）|
| flow-task-core executor 字段缺失（cost 落账死代码）| medium | add_task 记录加 executor 字段（enqueue 写入）|
| local/run.sh think 任务书声明大小写不一致 | medium | sed 归一化（[Tt]rue/[Ff]alse → \L\1）|

## 未修（落盘，明早 review 决策）

| finding | 级别 | 说明 |
|---|---|---|
| script 黑名单本质是防误触非防恶意（bash 注入难根治）| security high | 文档已注明；完整沙箱（bwrap/容器）需另立任务 |
| local/run.sh REDACTED_LOGS 直取 curl stderr | medium | 本地工具低风险；可后续 sanitize |
| local/run.sh 临时文件 .taskbook.txt/.run.out 含原始内容 | medium | 已有 TMP_ARTIFACTS 清理（退出即删）；权限 600 可加 |
| local/run.sh curl 错误分类（HTTP 错误算空回复）| medium | 加 --fail 区分 HTTP 错误 |
| local/run.sh 注释与行为不符（不可重试判定）| medium | 注释修正 |
| spec.yaml 契约不一致（OLLAMA_MODEL vs LOCAL_MODEL_DEFAULT）| low | spec 文档对齐 run.sh 实现 |
| script/run.sh workdir 参数未用 | low | 执行已 cd workdir；参数保留作契约 |
| 其他 low（test 断言/文档）| low | — |
| flow-task-core 既有 ruff 5 处（BLE001×2/S110/SIM102×2）| — | 设计语义，不动 |
