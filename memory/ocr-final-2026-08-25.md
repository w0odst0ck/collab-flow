# ocr 最终轮 findings（2026-08-25 闭环）

- 时间：23:52-23:57（final-closeout cron 触发）
- 范围：round1-10 修复 + cost-opt（model-gating/cache-dispatch）全部累积 diff，19 文件
- 结果：**4 findings（0 high / 3 medium / 1 low）**，EXIT=0
- 处理：按用户 23:30 拍板「最终 ocr 只跑一次，findings 全落盘不修，00:15 硬停」——**全部记录不修**，明早验收
- 原始输出：`memory/ocr-final-2026-08-25.json`（ocr 会话 dc0f4af3）

## Findings 清单

| # | 位置 | 级别 | 类型 | 摘要 |
|---|------|------|------|------|
| 1 | executors/reasonix/wrapper.sh:63-65 | medium | maintainability | 任务书 `model:` 声明被 flow-core W-S1 size-gating 注入的 `--model`（CLI 最高优先级）遮蔽，TB_MODEL_DECL 分支在 flow workitem execute 路径不可达；需确认优先级或文档化 |
| 2 | scripts/flow-task-core.py:3088-3089 | medium | other | assess_execute_completion 持 .lock 时 result.json 缺失/损坏 → `_run_tests` 无超时 subprocess(shell=True) 在锁内跑，阻塞该 workitem 全部转移；建议移出锁或加 bounded timeout |
| 3 | scripts/flow-core.py:3423-3428 | low | performance | resolve_execute_params 在 enqueue 路径被调两次（直接 + build_execute_command 内部），重复 design.md I/O + config 解析；建议复用已解析参数 |
| 4 | scripts/flow-task-core.py:2652-2654 | medium | bug | execute 任务终态钩子缺与 design 分支等价的状态守门（`cur == "executed"` 检查），可能对已转移 workitem 重复触发 auto_verify（重复 LLM/测试）+ spurious auto_verify_error audit；建议先读 state 仅 executed 时触发 |

## 明早验收建议
- 3 个 medium 建议开工即排（#1 优先级语义、#2 锁内无超时 subprocess、#4 终态钩子状态守门）
- #3 low 可攒批
